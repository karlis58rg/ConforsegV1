<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.ssp.conforseg">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Conforseg"
        android:usesCleartextTraffic="true">

        <activity android:name=".Activity.IncidenciasPendientes"
            android:launchMode="singleTask"/>
        <activity android:name=".Activity.EvidenciaMultimedia"
            android:launchMode="singleTask"/>
        <activity android:name=".Activity.Asistencia"
            android:launchMode="singleTask"/>
        <activity android:name=".Activity.Incidencias"
            android:launchMode="singleTask"/>
        <activity android:name=".Activity.Supervisiones"
            android:launchMode="singleTask"/>
        <activity
            android:name=".Fragment.Servicios"
            android:label="@string/title_activity_servicios"
            android:theme="@style/Theme.Conforseg.NoActionBar"
            android:launchMode="singleTask"/>
        <activity android:name=".Activity.Supervision"
            android:launchMode="singleTask"/>
        <activity android:name=".Activity.Login"
            android:launchMode="singleTask"/>
        <activity
            android:name=".ui.login.Login"
            android:label="@string/title_activity_login" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>